# Kavya's graduation card

A mobile-first scroll surprise and printable 8.5×11 graduation card.

Open `index.html` locally, or serve the repository with any static host. The **Save as a card** button opens the browser print dialog; choose **Save as PDF** for the Letter-size download.
